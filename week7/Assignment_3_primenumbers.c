#include <stdio.h>
#include <math.h>

void main(){
    int n;
    scanf("%d", &n);
    for (int i = 0; i < 0; i++){
        
        for (int j = 3; j < sqrt(i); j += 2){

        }

    }
}